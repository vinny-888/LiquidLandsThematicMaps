const HOUR = 1000*60*60;
const guarded_hours = HOUR*120;
const guarded_yield_hours = HOUR*24;
const realmHeightRatio = 1.142;
const queryString = window.location.search;
const urlParams = new URLSearchParams(queryString);